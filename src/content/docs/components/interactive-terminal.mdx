---
title: 🎮 Interactive Terminal
description: Terminal with clickable command buttons
icon: MousePointer
full: true
---

import { Preview } from "@/components/mdx/preview";
import { InteractiveTerminal } from "@/components/code-clamp/terminal";

# 🎮 Interactive Terminal

A terminal interface with clickable command buttons for easier interaction, perfect for demos and guided experiences.

<Preview
  link="interactive-terminal"
  comment={["Interactive"]}
  className="bg-card border rounded-xl p-6"
>
  <InteractiveTerminal 
    welcomeMessage="Welcome to Interactive Terminal. Click the buttons below or type commands."
    options={[
      { label: "Hello", value: "hello", description: "Display a greeting" },
      { label: "Date", value: "date", description: "Show current date and time" },
      { label: "Help", value: "help", description: "Show available commands" }
    ]}
    onCommand={async (cmd) => {
      if (cmd === "hello") {
        return "Hello, world!";
      } else if (cmd === "date") {
        return new Date().toString();
      } else if (cmd === "help") {
        return [
          "Available commands:",
          "hello - Display a greeting",
          "date - Show current date and time",
          "help - Show this help message"
        ];
      } else {
        return `Command not found: ${cmd}`;
      }
    }}
  />
</Preview>

## ✨ Features

<div className="grid grid-cols-1 md:grid-cols-2 gap-4 mt-6">
   <div className="p-4 rounded-lg border bg-card">
    <h3 className="font-semibold flex items-center gap-2">
      <span className="text-primary">•</span> Command Buttons
    </h3>
    <p className="mt-2 text-sm text-muted-foreground">
      Clickable buttons for common commands
    </p>
  </div>
   <div className="p-4 rounded-lg border bg-card">
    <h3 className="flex items-center gap-2 font-semibold">
      <span className="text-primary">•</span> Collapsible UI
    </h3>
    <p className="text-muted-foreground mt-2 text-sm">
      Toggle command panel visibility
    </p>
  </div>

   <div className="p-4 rounded-lg border bg-card">
    <h3 className="flex items-center gap-2 font-semibold">
      <span className="text-primary">•</span> Command Tooltips
    </h3>
    <p className="text-muted-foreground mt-2 text-sm">
      Helpful descriptions for available commands
    </p>
  </div>

  <div className="p-4 rounded-lg border bg-card">
    <h3 className="font-semibold flex items-center gap-2">
      <span className="text-primary">•</span> Utility Buttons
    </h3>
    <p className="mt-2 text-sm text-muted-foreground">
      Quick access to help and clear functions
    </p>
  </div>
</div>

## 📦 Installation

First install required dependencies:

\`\`\`bash
bun add framer-motion lucide-react
# or
npm install framer-motion lucide-react
# or
yarn add framer-motion lucide-react
\`\`\`


## 🔧 Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `className` | `string` | `undefined` | Additional CSS classes |
| `options` | `InteractiveOption[]` | `[]` | Command buttons to display |
| `showHelpButton` | `boolean` | `true` | Show the help button |
| `showClearButton` | `boolean` | `true` | Show the clear button |
| `onCommand` | `(command: string) => Promise<string \| string[]> \| string \| string[]` | `undefined` | Command handler function |

All props from `BaseTerminal` are also supported.


