---
title: 🧩 Matrix Terminal
description: Terminal with falling code animation background
icon: Code
full: true
---

import { Preview } from "@/components/mdx/preview";
import { MatrixTerminal } from "@/components/code-clamp/terminal";

# 🧩 Matrix Terminal

A terminal interface with a "Matrix-style" falling code animation in the background.

<Preview
  link="matrix-terminal"
  comment={["Animation"]}
  className="bg-card border rounded-xl p-6"
>
  <MatrixTerminal 
    welcomeMessage={[
      "Matrix Terminal Initialized",
      "System breach detected...",
      "Type 'connect' to establish secure connection"
    ]}
    onCommand={async (cmd) => {
      if (cmd === "connect") {
        return [
          "Establishing secure connection...",
          "Access granted.",
          "Welcome to the Matrix."
        ];
      } else {
        return `Unknown command: ${cmd}`;
      }
    }}
  />
</Preview>

## ✨ Features

<div className="grid grid-cols-1 md:grid-cols-2 gap-4 mt-6">
  <div className="p-4 border rounded-lg bg-card">
    <h3 className="font-semibold flex items-center gap-2">
      <span className="text-primary">•</span> Falling Code Animation
    </h3>
    <p className="mt-2 text-sm text-muted-foreground">
      Dynamic background with Matrix-inspired falling characters
    </p>
  </div>
   <div className="p-4 border rounded-lg bg-card">
    <h3 className="flex items-center gap-2 font-semibold">
      <span className="text-primary">•</span> Canvas Rendering
    </h3>
    <p className="text-muted-foreground mt-2 text-sm">
      Efficient HTML5 Canvas-based animation
    </p>
  </div>

   <div className="p-4 border rounded-lg bg-card">
    <h3 className="flex items-center gap-2 font-semibold">
      <span className="text-primary">•</span> Responsive Design
    </h3>
    <p className="text-muted-foreground mt-2 text-sm">
      Automatically adapts to container size
    </p>
  </div>

   <div className="p-4 border rounded-lg bg-card">
    <h3 className="font-semibold flex items-center gap-2">
      <span className="text-primary">•</span> Customizable Animation
    </h3>
    <p className="mt-2 text-sm text-muted-foreground">
      Adjustable density and speed parameters
    </p>
  </div>
</div>

## 📦 Installation

First install required dependencies:

\`\`\`bash
bun add framer-motion lucide-react
# or
npm install framer-motion lucide-react
# or
yarn add framer-motion lucide-react
\`\`\`

## 🔧 Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `className` | `string` | `undefined` | Additional CSS classes |
| `matrixDensity` | `number` | `50` | Density of falling characters |
| `matrixSpeed` | `number` | `1` | Speed of animation |
| `prompt` | `string` | `>` | Command prompt symbol |
| `welcomeMessage` | `string \| string[]` | `Matrix Terminal Initialized...` | Initial message shown |
| `onCommand` | `(command: string) => Promise<string \| string[]> \| string \| string[]` | `undefined` | Command handler function |

All props from `BaseTerminal` are also supported.


